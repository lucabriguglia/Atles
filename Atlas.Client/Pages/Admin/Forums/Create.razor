@page "/admin/forums/create"
@page "/admin/forums/create/{forumGroupId:guid}"
@using Atlas.Shared.Models.Admin.Forums
@attribute [Authorize(Policy = "Admin")]
@inject HttpClient Http
@inject NavigationManager NavigationManager

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/admin">Home</a></li>
        <li class="breadcrumb-item"><a href="/admin/forums">Forums</a></li>
        <li class="breadcrumb-item active" aria-current="page">@Title</li>
    </ol>
</nav>

<h3 class="mb-3">@Title</h3>

@if (Model == null)
{
    <Loading />
}
else
{
    <Form Model="Model" Button="Save" OnValidSubmit="@SaveAsync"></Form>
}

@code {
    private const string Title = "Create Forum";
    [Parameter] public Guid? ForumGroupId { get; set; } = null;
    private FormModel Model { get; set; }

    protected override async Task OnInitializedAsync()
    {
        var requestUri = ForumGroupId == null
            ? $"api/admin/forums/create"
            : $"api/admin/forums/create/{ForumGroupId}";

        Model = await Http.GetFromJsonAsync<FormModel>(requestUri);
    }

    private async Task SaveAsync()
    {
        await Http.PostAsJsonAsync($"api/admin/forums/save", Model.Forum);
        NavigationManager.NavigateTo($"/admin/forums/{Model.Forum.ForumGroupId}");
    }
}
