@page "/forum/{id:guid}"
@page "/forum/{slug}"

@inherits Atlify.Client.Components.Pages.ForumPage

@AddComponent("Forum", new Dictionary<string, object>
{
    { nameof(Id), Id },
    { nameof(Slug), Slug }
})