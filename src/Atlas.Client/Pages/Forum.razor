@page "/forum/{id:guid}"
@inherits ForumPage

@if (Model == null)
{
    <Loading />
}
else
{
    if (DisplayPage)
    {
        @AddComponent("ForumComponent", new Dictionary<string, object> { { "Model", Model}, { "Id", Id} })
    }
    else
    {
        <SomethingWrong />
    }
}