@page "/member/{id:guid}"
@inherits MemberPage

@if (Model == null)
{
    <Loading />
}
else
{
    @AddComponent("Member", new Dictionary<string, object>
    {
        { nameof(Model), Model }
    });
}