@page "/forum/{forumSlug}/{topicSlug}"

@inherits Atles.Client.Pages.TopicPage

@AddComponent("Topic", new Dictionary<string, object>
{
    { nameof(ForumSlug), ForumSlug },
    { nameof(TopicSlug), TopicSlug }
})
